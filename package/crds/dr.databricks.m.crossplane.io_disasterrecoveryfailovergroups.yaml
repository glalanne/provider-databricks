---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: disasterrecoveryfailovergroups.dr.databricks.m.crossplane.io
spec:
  group: dr.databricks.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - databricks
    kind: DisasterRecoveryFailoverGroup
    listKind: DisasterRecoveryFailoverGroupList
    plural: disasterrecoveryfailovergroups
    singular: disasterrecoveryfailovergroup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: DisasterRecoveryFailoverGroup is the Schema for the DisasterRecoveryFailoverGroups
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DisasterRecoveryFailoverGroupSpec defines the desired state
              of DisasterRecoveryFailoverGroup
            properties:
              forProvider:
                properties:
                  failoverGroupId:
                    description: |-
                      Client-provided identifier for the failover group. Used to construct the
                      resource name as {parent}/failover-groups/{failover_group_id}
                    type: string
                  initialPrimaryRegion:
                    description: |-
                      Initial primary region. Used only in Create requests to set the starting
                      primary region. Not returned in responses
                    type: string
                  parent:
                    description: 'The parent resource. Format: accounts/{account_id}'
                    type: string
                  regions:
                    description: List of all regions participating in this failover
                      group
                    items:
                      type: string
                    type: array
                  unityCatalogAssets:
                    description: Unity Catalog replication configuration
                    properties:
                      catalogs:
                        description: UC catalogs to replicate
                        items:
                          properties:
                            name:
                              description: Resource name for this location
                              type: string
                          type: object
                        type: array
                      dataReplicationWorkspaceSet:
                        description: |-
                          The workspace set whose workspaces will be used for data replication
                          of all UC catalogs' underlying storage
                        type: string
                      locationMappings:
                        description: Location mappings - storage URI per region for
                          each location
                        items:
                          properties:
                            name:
                              description: Resource name for this location
                              type: string
                            uriByRegion:
                              description: URI for each region. Each entry maps a
                                region name to a storage URI
                              items:
                                properties:
                                  region:
                                    description: The region name
                                    type: string
                                  uri:
                                    description: The storage URI for this region
                                    type: string
                                type: object
                              type: array
                          type: object
                        type: array
                    type: object
                  workspaceSets:
                    description: Workspace sets, each containing workspaces that replicate
                      to each other
                    items:
                      properties:
                        name:
                          description: Resource name for this location
                          type: string
                        replicateWorkspaceAssets:
                          description: |-
                            Whether to enable control plane DR (notebooks, jobs, clusters, etc.) for this set.
                            Defaults to false
                          type: boolean
                        stableUrlNames:
                          description: |-
                            Resource names of stable URLs associated with this workspace set.
                            Format: accounts/{account_id}/stable-urls/{stable_url_id}.
                            The referenced stable URLs must already exist (via CreateStableUrl)
                          items:
                            type: string
                          type: array
                        workspaceIds:
                          description: |-
                            Workspace IDs in this set. The system derives and validates regions.
                            All workspaces must be in the Mission Critical tier
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  failoverGroupId:
                    description: |-
                      Client-provided identifier for the failover group. Used to construct the
                      resource name as {parent}/failover-groups/{failover_group_id}
                    type: string
                  initialPrimaryRegion:
                    description: |-
                      Initial primary region. Used only in Create requests to set the starting
                      primary region. Not returned in responses
                    type: string
                  parent:
                    description: 'The parent resource. Format: accounts/{account_id}'
                    type: string
                  regions:
                    description: List of all regions participating in this failover
                      group
                    items:
                      type: string
                    type: array
                  unityCatalogAssets:
                    description: Unity Catalog replication configuration
                    properties:
                      catalogs:
                        description: UC catalogs to replicate
                        items:
                          properties:
                            name:
                              description: Resource name for this location
                              type: string
                          type: object
                        type: array
                      dataReplicationWorkspaceSet:
                        description: |-
                          The workspace set whose workspaces will be used for data replication
                          of all UC catalogs' underlying storage
                        type: string
                      locationMappings:
                        description: Location mappings - storage URI per region for
                          each location
                        items:
                          properties:
                            name:
                              description: Resource name for this location
                              type: string
                            uriByRegion:
                              description: URI for each region. Each entry maps a
                                region name to a storage URI
                              items:
                                properties:
                                  region:
                                    description: The region name
                                    type: string
                                  uri:
                                    description: The storage URI for this region
                                    type: string
                                type: object
                              type: array
                          type: object
                        type: array
                    type: object
                  workspaceSets:
                    description: Workspace sets, each containing workspaces that replicate
                      to each other
                    items:
                      properties:
                        name:
                          description: Resource name for this location
                          type: string
                        replicateWorkspaceAssets:
                          description: |-
                            Whether to enable control plane DR (notebooks, jobs, clusters, etc.) for this set.
                            Defaults to false
                          type: boolean
                        stableUrlNames:
                          description: |-
                            Resource names of stable URLs associated with this workspace set.
                            Format: accounts/{account_id}/stable-urls/{stable_url_id}.
                            The referenced stable URLs must already exist (via CreateStableUrl)
                          items:
                            type: string
                          type: array
                        workspaceIds:
                          description: |-
                            Workspace IDs in this set. The system derives and validates regions.
                            All workspaces must be in the Mission Critical tier
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.failoverGroupId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.failoverGroupId)
                || (has(self.initProvider) && has(self.initProvider.failoverGroupId))'
            - message: spec.forProvider.initialPrimaryRegion is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.initialPrimaryRegion)
                || (has(self.initProvider) && has(self.initProvider.initialPrimaryRegion))'
            - message: spec.forProvider.parent is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.parent)
                || (has(self.initProvider) && has(self.initProvider.parent))'
            - message: spec.forProvider.regions is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.regions)
                || (has(self.initProvider) && has(self.initProvider.regions))'
            - message: spec.forProvider.workspaceSets is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.workspaceSets)
                || (has(self.initProvider) && has(self.initProvider.workspaceSets))'
          status:
            description: DisasterRecoveryFailoverGroupStatus defines the observed
              state of DisasterRecoveryFailoverGroup.
            properties:
              atProvider:
                properties:
                  createTime:
                    description: Time at which this failover group was created
                    type: string
                  effectivePrimaryRegion:
                    description: |-
                      Current effective primary region. Replication flows FROM workspaces in this region.
                      Changes after a successful failover
                    type: string
                  etag:
                    description: Opaque version string for optimistic locking. Server-generated
                      and returned in responses
                    type: string
                  failoverGroupId:
                    description: |-
                      Client-provided identifier for the failover group. Used to construct the
                      resource name as {parent}/failover-groups/{failover_group_id}
                    type: string
                  id:
                    type: string
                  initialPrimaryRegion:
                    description: |-
                      Initial primary region. Used only in Create requests to set the starting
                      primary region. Not returned in responses
                    type: string
                  name:
                    description: Resource name for this location
                    type: string
                  parent:
                    description: 'The parent resource. Format: accounts/{account_id}'
                    type: string
                  regions:
                    description: List of all regions participating in this failover
                      group
                    items:
                      type: string
                    type: array
                  replicationPoint:
                    description: The latest point in time to which data has been replicated
                    type: string
                  state:
                    description: 'Aggregate state of the failover group. Possible
                      values are: ACTIVE, CREATING, CREATION_FAILED, DELETING, DELETION_FAILED,
                      FAILING_OVER, FAILOVER_FAILED, INITIAL_REPLICATION'
                    type: string
                  unityCatalogAssets:
                    description: Unity Catalog replication configuration
                    properties:
                      catalogs:
                        description: UC catalogs to replicate
                        items:
                          properties:
                            name:
                              description: Resource name for this location
                              type: string
                          type: object
                        type: array
                      dataReplicationWorkspaceSet:
                        description: |-
                          The workspace set whose workspaces will be used for data replication
                          of all UC catalogs' underlying storage
                        type: string
                      locationMappings:
                        description: Location mappings - storage URI per region for
                          each location
                        items:
                          properties:
                            name:
                              description: Resource name for this location
                              type: string
                            uriByRegion:
                              description: URI for each region. Each entry maps a
                                region name to a storage URI
                              items:
                                properties:
                                  region:
                                    description: The region name
                                    type: string
                                  uri:
                                    description: The storage URI for this region
                                    type: string
                                type: object
                              type: array
                          type: object
                        type: array
                    type: object
                  updateTime:
                    description: Time at which this failover group was last modified
                    type: string
                  workspaceSets:
                    description: Workspace sets, each containing workspaces that replicate
                      to each other
                    items:
                      properties:
                        name:
                          description: Resource name for this location
                          type: string
                        replicateWorkspaceAssets:
                          description: |-
                            Whether to enable control plane DR (notebooks, jobs, clusters, etc.) for this set.
                            Defaults to false
                          type: boolean
                        stableUrlNames:
                          description: |-
                            Resource names of stable URLs associated with this workspace set.
                            Format: accounts/{account_id}/stable-urls/{stable_url_id}.
                            The referenced stable URLs must already exist (via CreateStableUrl)
                          items:
                            type: string
                          type: array
                        workspaceIds:
                          description: |-
                            Workspace IDs in this set. The system derives and validates regions.
                            All workspaces must be in the Mission Critical tier
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
