---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: policyinfoes.unity.databricks.crossplane.io
spec:
  group: unity.databricks.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - databricks
    kind: PolicyInfo
    listKind: PolicyInfoList
    plural: policyinfoes
    singular: policyinfo
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: PolicyInfo is the Schema for the PolicyInfos API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PolicyInfoSpec defines the desired state of PolicyInfo
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  columnMask:
                    description: |-
                      Options for column mask policies. Valid only if policy_type is POLICY_TYPE_COLUMN_MASK.
                      Required on create and optional on update. When specified on update,
                      the new options will replace the existing options as a whole
                    properties:
                      functionName:
                        description: |-
                          The fully qualified name of the column mask function.
                          The function is called on each row of the target table.
                          The function's first argument and its return type should match the type of the masked column.
                          Required on create and update
                        type: string
                      onColumn:
                        description: |-
                          The alias of the column to be masked. The alias must refer to one of matched columns.
                          The values of the column is passed to the column mask function as the first argument.
                          Required on create and update
                        type: string
                      using:
                        description: |-
                          Optional list of column aliases or constant literals to be passed as additional arguments to the column mask function.
                          The type of each column should match the positional argument of the column mask function
                        items:
                          properties:
                            alias:
                              description: The alias of a matched column
                              type: string
                            constant:
                              description: A constant literal
                              type: string
                          type: object
                        type: array
                    type: object
                  comment:
                    description: Optional description of the policy
                    type: string
                  exceptPrincipals:
                    description: Optional list of user or group names that should
                      be excluded from the policy
                    items:
                      type: string
                    type: array
                  forSecurableType:
                    description: |-
                      Type of securables that the policy should take effect on.
                      Only TABLE is supported at this moment.
                      Required on create and optional on update. Possible values are: CATALOG, CLEAN_ROOM, CONNECTION, CREDENTIAL, EXTERNAL_LOCATION, EXTERNAL_METADATA, FUNCTION, METASTORE, PIPELINE, PROVIDER, RECIPIENT, SCHEMA, SHARE, STAGING_TABLE, STORAGE_CREDENTIAL, TABLE, VOLUME
                    type: string
                  matchColumns:
                    description: |-
                      Optional list of condition expressions used to match table columns.
                      Only valid when for_securable_type is TABLE.
                      When specified, the policy only applies to tables whose columns satisfy all match conditions
                    items:
                      properties:
                        alias:
                          description: The alias of a matched column
                          type: string
                        condition:
                          description: The condition expression used to match a table
                            column
                          type: string
                      type: object
                    type: array
                  onSecurableFullname:
                    description: |-
                      Full name of the securable on which the policy is defined.
                      Required on create
                    type: string
                  onSecurableType:
                    description: |-
                      Type of the securable on which the policy is defined.
                      Only CATALOG, SCHEMA and TABLE are supported at this moment.
                      Required on create. Possible values are: CATALOG, CLEAN_ROOM, CONNECTION, CREDENTIAL, EXTERNAL_LOCATION, EXTERNAL_METADATA, FUNCTION, METASTORE, PIPELINE, PROVIDER, RECIPIENT, SCHEMA, SHARE, STAGING_TABLE, STORAGE_CREDENTIAL, TABLE, VOLUME
                    type: string
                  policyType:
                    description: 'Type of the policy. Required on create. Possible
                      values are: POLICY_TYPE_COLUMN_MASK, POLICY_TYPE_ROW_FILTER'
                    type: string
                  providerConfig:
                    description: Configure the provider for management through account
                      provider.
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                  rowFilter:
                    description: |-
                      Options for row filter policies. Valid only if policy_type is POLICY_TYPE_ROW_FILTER.
                      Required on create and optional on update. When specified on update,
                      the new options will replace the existing options as a whole
                    properties:
                      functionName:
                        description: |-
                          The fully qualified name of the column mask function.
                          The function is called on each row of the target table.
                          The function's first argument and its return type should match the type of the masked column.
                          Required on create and update
                        type: string
                      using:
                        description: |-
                          Optional list of column aliases or constant literals to be passed as additional arguments to the column mask function.
                          The type of each column should match the positional argument of the column mask function
                        items:
                          properties:
                            alias:
                              description: The alias of a matched column
                              type: string
                            constant:
                              description: A constant literal
                              type: string
                          type: object
                        type: array
                    type: object
                  toPrincipals:
                    description: |-
                      List of user or group names that the policy applies to.
                      Required on create and optional on update
                    items:
                      type: string
                    type: array
                  whenCondition:
                    description: Optional condition when the policy should take effect
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  columnMask:
                    description: |-
                      Options for column mask policies. Valid only if policy_type is POLICY_TYPE_COLUMN_MASK.
                      Required on create and optional on update. When specified on update,
                      the new options will replace the existing options as a whole
                    properties:
                      functionName:
                        description: |-
                          The fully qualified name of the column mask function.
                          The function is called on each row of the target table.
                          The function's first argument and its return type should match the type of the masked column.
                          Required on create and update
                        type: string
                      onColumn:
                        description: |-
                          The alias of the column to be masked. The alias must refer to one of matched columns.
                          The values of the column is passed to the column mask function as the first argument.
                          Required on create and update
                        type: string
                      using:
                        description: |-
                          Optional list of column aliases or constant literals to be passed as additional arguments to the column mask function.
                          The type of each column should match the positional argument of the column mask function
                        items:
                          properties:
                            alias:
                              description: The alias of a matched column
                              type: string
                            constant:
                              description: A constant literal
                              type: string
                          type: object
                        type: array
                    type: object
                  comment:
                    description: Optional description of the policy
                    type: string
                  exceptPrincipals:
                    description: Optional list of user or group names that should
                      be excluded from the policy
                    items:
                      type: string
                    type: array
                  forSecurableType:
                    description: |-
                      Type of securables that the policy should take effect on.
                      Only TABLE is supported at this moment.
                      Required on create and optional on update. Possible values are: CATALOG, CLEAN_ROOM, CONNECTION, CREDENTIAL, EXTERNAL_LOCATION, EXTERNAL_METADATA, FUNCTION, METASTORE, PIPELINE, PROVIDER, RECIPIENT, SCHEMA, SHARE, STAGING_TABLE, STORAGE_CREDENTIAL, TABLE, VOLUME
                    type: string
                  matchColumns:
                    description: |-
                      Optional list of condition expressions used to match table columns.
                      Only valid when for_securable_type is TABLE.
                      When specified, the policy only applies to tables whose columns satisfy all match conditions
                    items:
                      properties:
                        alias:
                          description: The alias of a matched column
                          type: string
                        condition:
                          description: The condition expression used to match a table
                            column
                          type: string
                      type: object
                    type: array
                  onSecurableFullname:
                    description: |-
                      Full name of the securable on which the policy is defined.
                      Required on create
                    type: string
                  onSecurableType:
                    description: |-
                      Type of the securable on which the policy is defined.
                      Only CATALOG, SCHEMA and TABLE are supported at this moment.
                      Required on create. Possible values are: CATALOG, CLEAN_ROOM, CONNECTION, CREDENTIAL, EXTERNAL_LOCATION, EXTERNAL_METADATA, FUNCTION, METASTORE, PIPELINE, PROVIDER, RECIPIENT, SCHEMA, SHARE, STAGING_TABLE, STORAGE_CREDENTIAL, TABLE, VOLUME
                    type: string
                  policyType:
                    description: 'Type of the policy. Required on create. Possible
                      values are: POLICY_TYPE_COLUMN_MASK, POLICY_TYPE_ROW_FILTER'
                    type: string
                  providerConfig:
                    description: Configure the provider for management through account
                      provider.
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                  rowFilter:
                    description: |-
                      Options for row filter policies. Valid only if policy_type is POLICY_TYPE_ROW_FILTER.
                      Required on create and optional on update. When specified on update,
                      the new options will replace the existing options as a whole
                    properties:
                      functionName:
                        description: |-
                          The fully qualified name of the column mask function.
                          The function is called on each row of the target table.
                          The function's first argument and its return type should match the type of the masked column.
                          Required on create and update
                        type: string
                      using:
                        description: |-
                          Optional list of column aliases or constant literals to be passed as additional arguments to the column mask function.
                          The type of each column should match the positional argument of the column mask function
                        items:
                          properties:
                            alias:
                              description: The alias of a matched column
                              type: string
                            constant:
                              description: A constant literal
                              type: string
                          type: object
                        type: array
                    type: object
                  toPrincipals:
                    description: |-
                      List of user or group names that the policy applies to.
                      Required on create and optional on update
                    items:
                      type: string
                    type: array
                  whenCondition:
                    description: Optional condition when the policy should take effect
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.forSecurableType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.forSecurableType)
                || (has(self.initProvider) && has(self.initProvider.forSecurableType))'
            - message: spec.forProvider.policyType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.policyType)
                || (has(self.initProvider) && has(self.initProvider.policyType))'
            - message: spec.forProvider.toPrincipals is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.toPrincipals)
                || (has(self.initProvider) && has(self.initProvider.toPrincipals))'
          status:
            description: PolicyInfoStatus defines the observed state of PolicyInfo.
            properties:
              atProvider:
                properties:
                  columnMask:
                    description: |-
                      Options for column mask policies. Valid only if policy_type is POLICY_TYPE_COLUMN_MASK.
                      Required on create and optional on update. When specified on update,
                      the new options will replace the existing options as a whole
                    properties:
                      functionName:
                        description: |-
                          The fully qualified name of the column mask function.
                          The function is called on each row of the target table.
                          The function's first argument and its return type should match the type of the masked column.
                          Required on create and update
                        type: string
                      onColumn:
                        description: |-
                          The alias of the column to be masked. The alias must refer to one of matched columns.
                          The values of the column is passed to the column mask function as the first argument.
                          Required on create and update
                        type: string
                      using:
                        description: |-
                          Optional list of column aliases or constant literals to be passed as additional arguments to the column mask function.
                          The type of each column should match the positional argument of the column mask function
                        items:
                          properties:
                            alias:
                              description: The alias of a matched column
                              type: string
                            constant:
                              description: A constant literal
                              type: string
                          type: object
                        type: array
                    type: object
                  comment:
                    description: Optional description of the policy
                    type: string
                  createdAt:
                    description: Time at which the policy was created, in epoch milliseconds.
                      Output only
                    type: number
                  createdBy:
                    description: Username of the user who created the policy. Output
                      only
                    type: string
                  exceptPrincipals:
                    description: Optional list of user or group names that should
                      be excluded from the policy
                    items:
                      type: string
                    type: array
                  forSecurableType:
                    description: |-
                      Type of securables that the policy should take effect on.
                      Only TABLE is supported at this moment.
                      Required on create and optional on update. Possible values are: CATALOG, CLEAN_ROOM, CONNECTION, CREDENTIAL, EXTERNAL_LOCATION, EXTERNAL_METADATA, FUNCTION, METASTORE, PIPELINE, PROVIDER, RECIPIENT, SCHEMA, SHARE, STAGING_TABLE, STORAGE_CREDENTIAL, TABLE, VOLUME
                    type: string
                  id:
                    description: Unique identifier of the policy. This field is output
                      only and is generated by the system
                    type: string
                  matchColumns:
                    description: |-
                      Optional list of condition expressions used to match table columns.
                      Only valid when for_securable_type is TABLE.
                      When specified, the policy only applies to tables whose columns satisfy all match conditions
                    items:
                      properties:
                        alias:
                          description: The alias of a matched column
                          type: string
                        condition:
                          description: The condition expression used to match a table
                            column
                          type: string
                      type: object
                    type: array
                  onSecurableFullname:
                    description: |-
                      Full name of the securable on which the policy is defined.
                      Required on create
                    type: string
                  onSecurableType:
                    description: |-
                      Type of the securable on which the policy is defined.
                      Only CATALOG, SCHEMA and TABLE are supported at this moment.
                      Required on create. Possible values are: CATALOG, CLEAN_ROOM, CONNECTION, CREDENTIAL, EXTERNAL_LOCATION, EXTERNAL_METADATA, FUNCTION, METASTORE, PIPELINE, PROVIDER, RECIPIENT, SCHEMA, SHARE, STAGING_TABLE, STORAGE_CREDENTIAL, TABLE, VOLUME
                    type: string
                  policyType:
                    description: 'Type of the policy. Required on create. Possible
                      values are: POLICY_TYPE_COLUMN_MASK, POLICY_TYPE_ROW_FILTER'
                    type: string
                  providerConfig:
                    description: Configure the provider for management through account
                      provider.
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                  rowFilter:
                    description: |-
                      Options for row filter policies. Valid only if policy_type is POLICY_TYPE_ROW_FILTER.
                      Required on create and optional on update. When specified on update,
                      the new options will replace the existing options as a whole
                    properties:
                      functionName:
                        description: |-
                          The fully qualified name of the column mask function.
                          The function is called on each row of the target table.
                          The function's first argument and its return type should match the type of the masked column.
                          Required on create and update
                        type: string
                      using:
                        description: |-
                          Optional list of column aliases or constant literals to be passed as additional arguments to the column mask function.
                          The type of each column should match the positional argument of the column mask function
                        items:
                          properties:
                            alias:
                              description: The alias of a matched column
                              type: string
                            constant:
                              description: A constant literal
                              type: string
                          type: object
                        type: array
                    type: object
                  toPrincipals:
                    description: |-
                      List of user or group names that the policy applies to.
                      Required on create and optional on update
                    items:
                      type: string
                    type: array
                  updatedAt:
                    description: Time at which the policy was last modified, in epoch
                      milliseconds. Output only
                    type: number
                  updatedBy:
                    description: Username of the user who last modified the policy.
                      Output only
                    type: string
                  whenCondition:
                    description: Optional condition when the policy should take effect
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
