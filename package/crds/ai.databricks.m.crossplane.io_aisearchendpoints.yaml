---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: aisearchendpoints.ai.databricks.m.crossplane.io
spec:
  group: ai.databricks.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - databricks
    kind: AISearchEndpoint
    listKind: AISearchEndpointList
    plural: aisearchendpoints
    singular: aisearchendpoint
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: AISearchEndpoint is the Schema for the AISearchEndpoints API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AISearchEndpointSpec defines the desired state of AISearchEndpoint
            properties:
              forProvider:
                properties:
                  budgetPolicyId:
                    description: The user-selected budget policy id for the endpoint
                    type: string
                  customTags:
                    description: The custom tags assigned to the endpoint
                    items:
                      properties:
                        key:
                          description: Key field for an AI Search endpoint tag
                          type: string
                        value:
                          description: '[Optional] Value field for an AI Search endpoint
                            tag'
                          type: string
                      type: object
                    type: array
                  endpointId:
                    description: |-
                      The user-supplied short name for the Endpoint, per AIP-133. The server composes the
                      full Endpoint.name as {parent}/endpoints/{endpoint_id}. AIP-133 does not list
                      endpoint_id as a fields-may-be-required entry, so we annotate it OPTIONAL on the
                      wire; the server still rejects empty values with INVALID_PARAMETER_VALUE
                    type: string
                  endpointType:
                    description: 'Type of endpoint. Required on create and immutable
                      thereafter. Possible values are: STANDARD, STORAGE_OPTIMIZED'
                    type: string
                  parent:
                    description: |-
                      The Workspace where this Endpoint will be created.
                      Format: workspaces/{workspace_id}
                    type: string
                  providerConfig:
                    description: Configure the provider for management through account
                      provider.
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                  replicaCount:
                    description: |-
                      The client-supplied desired number of replicas for the endpoint, applied at
                      create/update time. Mutually exclusive with target_qps
                    type: number
                  targetQps:
                    description: |-
                      Target QPS for the endpoint. Mutually exclusive with replica_count. Best-effort;
                      the system does not guarantee this QPS will be achieved
                    type: number
                  usagePolicyId:
                    description: The usage policy id applied to the endpoint
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  budgetPolicyId:
                    description: The user-selected budget policy id for the endpoint
                    type: string
                  customTags:
                    description: The custom tags assigned to the endpoint
                    items:
                      properties:
                        key:
                          description: Key field for an AI Search endpoint tag
                          type: string
                        value:
                          description: '[Optional] Value field for an AI Search endpoint
                            tag'
                          type: string
                      type: object
                    type: array
                  endpointId:
                    description: |-
                      The user-supplied short name for the Endpoint, per AIP-133. The server composes the
                      full Endpoint.name as {parent}/endpoints/{endpoint_id}. AIP-133 does not list
                      endpoint_id as a fields-may-be-required entry, so we annotate it OPTIONAL on the
                      wire; the server still rejects empty values with INVALID_PARAMETER_VALUE
                    type: string
                  endpointType:
                    description: 'Type of endpoint. Required on create and immutable
                      thereafter. Possible values are: STANDARD, STORAGE_OPTIMIZED'
                    type: string
                  parent:
                    description: |-
                      The Workspace where this Endpoint will be created.
                      Format: workspaces/{workspace_id}
                    type: string
                  providerConfig:
                    description: Configure the provider for management through account
                      provider.
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                  replicaCount:
                    description: |-
                      The client-supplied desired number of replicas for the endpoint, applied at
                      create/update time. Mutually exclusive with target_qps
                    type: number
                  targetQps:
                    description: |-
                      Target QPS for the endpoint. Mutually exclusive with replica_count. Best-effort;
                      the system does not guarantee this QPS will be achieved
                    type: number
                  usagePolicyId:
                    description: The usage policy id applied to the endpoint
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.endpointType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.endpointType)
                || (has(self.initProvider) && has(self.initProvider.endpointType))'
            - message: spec.forProvider.parent is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.parent)
                || (has(self.initProvider) && has(self.initProvider.parent))'
          status:
            description: AISearchEndpointStatus defines the observed state of AISearchEndpoint.
            properties:
              atProvider:
                properties:
                  budgetPolicyId:
                    description: The user-selected budget policy id for the endpoint
                    type: string
                  createTime:
                    description: Time the endpoint was created
                    type: string
                  creator:
                    description: Creator of the endpoint
                    type: string
                  customTags:
                    description: The custom tags assigned to the endpoint
                    items:
                      properties:
                        key:
                          description: Key field for an AI Search endpoint tag
                          type: string
                        value:
                          description: '[Optional] Value field for an AI Search endpoint
                            tag'
                          type: string
                      type: object
                    type: array
                  effectiveBudgetPolicyId:
                    description: The budget policy id applied to the endpoint
                    type: string
                  endpointId:
                    description: |-
                      The user-supplied short name for the Endpoint, per AIP-133. The server composes the
                      full Endpoint.name as {parent}/endpoints/{endpoint_id}. AIP-133 does not list
                      endpoint_id as a fields-may-be-required entry, so we annotate it OPTIONAL on the
                      wire; the server still rejects empty values with INVALID_PARAMETER_VALUE
                    type: string
                  endpointStatus:
                    description: Current status of the endpoint
                    properties:
                      message:
                        description: Human-readable detail about the endpoint's current
                          state or the reason for a state transition
                        type: string
                      state:
                        description: 'The current state of the scaling change request.
                          Possible values are: SCALING_CHANGE_APPLIED, SCALING_CHANGE_IN_PROGRESS,
                          SCALING_CHANGE_UNSPECIFIED'
                        type: string
                    type: object
                  endpointType:
                    description: 'Type of endpoint. Required on create and immutable
                      thereafter. Possible values are: STANDARD, STORAGE_OPTIMIZED'
                    type: string
                  id:
                    description: Unique identifier of the endpoint
                    type: string
                  indexCount:
                    description: Number of indexes on the endpoint
                    type: number
                  lastUpdatedUser:
                    description: User who last updated the endpoint
                    type: string
                  parent:
                    description: |-
                      The Workspace where this Endpoint will be created.
                      Format: workspaces/{workspace_id}
                    type: string
                  providerConfig:
                    description: Configure the provider for management through account
                      provider.
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                  replicaCount:
                    description: |-
                      The client-supplied desired number of replicas for the endpoint, applied at
                      create/update time. Mutually exclusive with target_qps
                    type: number
                  scalingInfo:
                    description: Scaling information for the endpoint
                    properties:
                      requestedTargetQps:
                        description: |-
                          The requested QPS target for the endpoint. Best-effort; the system does not
                          guarantee this QPS will be achieved
                        type: number
                      state:
                        description: 'The current state of the scaling change request.
                          Possible values are: SCALING_CHANGE_APPLIED, SCALING_CHANGE_IN_PROGRESS,
                          SCALING_CHANGE_UNSPECIFIED'
                        type: string
                    type: object
                  targetQps:
                    description: |-
                      Target QPS for the endpoint. Mutually exclusive with replica_count. Best-effort;
                      the system does not guarantee this QPS will be achieved
                    type: number
                  throughputInfo:
                    description: Throughput information for the endpoint
                    properties:
                      changeRequestMessage:
                        description: Additional information about the throughput change
                          request
                        type: string
                      changeRequestState:
                        description: 'The state of the most recent throughput change
                          request. Possible values are: CHANGE_ADJUSTED, CHANGE_FAILED,
                          CHANGE_IN_PROGRESS, CHANGE_REACHED_MAXIMUM, CHANGE_REACHED_MINIMUM,
                          CHANGE_SUCCESS'
                        type: string
                      currentConcurrency:
                        description: The current concurrency (total CPU) allocated
                          to the endpoint
                        type: number
                      currentConcurrencyUtilizationPercentage:
                        description: The current utilization of concurrency as a percentage
                          (0-100)
                        type: number
                      currentNumReplicas:
                        description: The current number of replicas allocated to the
                          endpoint
                        type: number
                      maximumConcurrencyAllowed:
                        description: The maximum concurrency allowed for this endpoint
                        type: number
                      minimalConcurrencyAllowed:
                        description: The minimum concurrency allowed for this endpoint
                        type: number
                      requestedConcurrency:
                        description: The requested concurrency (total CPU) for the
                          endpoint
                        type: number
                      requestedNumReplicas:
                        description: The requested number of replicas for the endpoint
                        type: number
                    type: object
                  updateTime:
                    description: Time the endpoint was last updated
                    type: string
                  usagePolicyId:
                    description: The usage policy id applied to the endpoint
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
