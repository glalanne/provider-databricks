---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: alertv2s.sql.databricks.crossplane.io
spec:
  group: sql.databricks.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - databricks
    kind: AlertV2
    listKind: AlertV2List
    plural: alertv2s
    singular: alertv2
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: AlertV2 is the Schema for the AlertV2s API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AlertV2Spec defines the desired state of AlertV2
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  customDescription:
                    description: Custom description for the alert. support mustache
                      template
                    type: string
                  customSummary:
                    description: Custom summary for the alert. support mustache template
                    type: string
                  displayName:
                    description: The display name of the alert
                    type: string
                  evaluation:
                    description: (AlertV2Evaluation, required)
                    properties:
                      comparisonOperator:
                        description: 'Operator used for comparison in alert evaluation.
                          Possible values are: EQUAL, GREATER_THAN, GREATER_THAN_OR_EQUAL,
                          IS_NOT_NULL, IS_NULL, LESS_THAN, LESS_THAN_OR_EQUAL, NOT_EQUAL'
                        type: string
                      emptyResultState:
                        description: 'Alert state if result is empty. Please avoid
                          setting this field to be UNKNOWN because UNKNOWN state is
                          planned to be deprecated. Possible values are: ERROR, OK,
                          TRIGGERED, UNKNOWN'
                        type: string
                      notification:
                        description: User or Notification Destination to notify when
                          alert is triggered
                        properties:
                          notifyOnOk:
                            description: Whether to notify alert subscribers when
                              alert returns back to normal
                            type: boolean
                          retriggerSeconds:
                            description: |-
                              Number of seconds an alert waits after being triggered before it is allowed to send another notification.
                              If set to 0 or omitted, the alert will not send any further notifications after the first trigger
                              Setting this value to 1 allows the alert to send a notification on every evaluation where the condition is met, effectively making it always retrigger for notification purposes
                            type: number
                          subscriptions:
                            description: (list of AlertV2Subscription, optional)
                            items:
                              properties:
                                destinationId:
                                  description: (string, optional)
                                  type: string
                                userEmail:
                                  description: (string, optional)
                                  type: string
                              type: object
                            type: array
                        type: object
                      source:
                        description: Source column from result to use to evaluate
                          alert
                        properties:
                          aggregation:
                            description: 'If not set, the behavior is equivalent to
                              using First row in the UI. Possible values are: AVG,
                              COUNT, COUNT_DISTINCT, MAX, MEDIAN, MIN, STDDEV, SUM'
                            type: string
                          display:
                            description: (string, optional)
                            type: string
                          name:
                            description: (string, required)
                            type: string
                        type: object
                      threshold:
                        description: Threshold to user for alert evaluation, can be
                          a column or a value
                        properties:
                          column:
                            description: (AlertV2OperandColumn, optional)
                            properties:
                              aggregation:
                                description: 'If not set, the behavior is equivalent
                                  to using First row in the UI. Possible values are:
                                  AVG, COUNT, COUNT_DISTINCT, MAX, MEDIAN, MIN, STDDEV,
                                  SUM'
                                type: string
                              display:
                                description: (string, optional)
                                type: string
                              name:
                                description: (string, required)
                                type: string
                            type: object
                          value:
                            description: (AlertV2OperandValue, optional)
                            properties:
                              boolValue:
                                description: (boolean, optional)
                                type: boolean
                              doubleValue:
                                description: (number, optional)
                                type: number
                              stringValue:
                                description: (string, optional)
                                type: string
                            type: object
                        type: object
                    type: object
                  parentPath:
                    description: The workspace path of the folder containing the alert.
                      Can only be set on create, and cannot be updated
                    type: string
                  providerConfig:
                    description: Configure the provider for management through account
                      provider.
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                  purgeOnDelete:
                    description: Whether to permanently delete the alert. If not set,
                      the alert will only be soft deleted
                    type: boolean
                  queryText:
                    description: Text of the query to be run
                    type: string
                  runAs:
                    description: |-
                      Specifies the identity that will be used to run the alert.
                      This field allows you to configure alerts to run as a specific user or service principal.
                    properties:
                      servicePrincipalName:
                        description: |-
                          to the application ID. Requires the servicePrincipal/user role.
                          If not specified, the alert will run as the request user
                        type: string
                      userName:
                        description: to the email of an active workspace user. Users
                          can only set this to their own email.
                        type: string
                    type: object
                  runAsUserName:
                    description: |-
                      The run as username or application ID of service principal.
                      On Create and Update, this field can be set to application ID of an active service principal. Setting this field requires the servicePrincipal/user role.
                      Deprecated: Use run_as field instead. This field will be removed in a future release
                    type: string
                  schedule:
                    description: (CronSchedule, required)
                    properties:
                      pauseStatus:
                        description: 'Indicate whether this schedule is paused or
                          not. Possible values are: PAUSED, UNPAUSED'
                        type: string
                      quartzCronSchedule:
                        description: |-
                          A cron expression using quartz syntax that specifies the schedule for this pipeline.
                          Should use the quartz format described here: http://www.quartz-scheduler.org/documentation/quartz-2.1.7/tutorials/tutorial-lesson-06.html
                        type: string
                      timezoneId:
                        description: |-
                          A Java timezone id. The schedule will be resolved using this timezone.
                          This will be combined with the quartz_cron_schedule to determine the schedule.
                          See https://docs.databricks.com/sql/language-manual/sql-ref-syntax-aux-conf-mgmt-set-timezone.html for details
                        type: string
                    type: object
                  warehouseId:
                    description: ID of the SQL warehouse attached to the alert
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  customDescription:
                    description: Custom description for the alert. support mustache
                      template
                    type: string
                  customSummary:
                    description: Custom summary for the alert. support mustache template
                    type: string
                  displayName:
                    description: The display name of the alert
                    type: string
                  evaluation:
                    description: (AlertV2Evaluation, required)
                    properties:
                      comparisonOperator:
                        description: 'Operator used for comparison in alert evaluation.
                          Possible values are: EQUAL, GREATER_THAN, GREATER_THAN_OR_EQUAL,
                          IS_NOT_NULL, IS_NULL, LESS_THAN, LESS_THAN_OR_EQUAL, NOT_EQUAL'
                        type: string
                      emptyResultState:
                        description: 'Alert state if result is empty. Please avoid
                          setting this field to be UNKNOWN because UNKNOWN state is
                          planned to be deprecated. Possible values are: ERROR, OK,
                          TRIGGERED, UNKNOWN'
                        type: string
                      notification:
                        description: User or Notification Destination to notify when
                          alert is triggered
                        properties:
                          notifyOnOk:
                            description: Whether to notify alert subscribers when
                              alert returns back to normal
                            type: boolean
                          retriggerSeconds:
                            description: |-
                              Number of seconds an alert waits after being triggered before it is allowed to send another notification.
                              If set to 0 or omitted, the alert will not send any further notifications after the first trigger
                              Setting this value to 1 allows the alert to send a notification on every evaluation where the condition is met, effectively making it always retrigger for notification purposes
                            type: number
                          subscriptions:
                            description: (list of AlertV2Subscription, optional)
                            items:
                              properties:
                                destinationId:
                                  description: (string, optional)
                                  type: string
                                userEmail:
                                  description: (string, optional)
                                  type: string
                              type: object
                            type: array
                        type: object
                      source:
                        description: Source column from result to use to evaluate
                          alert
                        properties:
                          aggregation:
                            description: 'If not set, the behavior is equivalent to
                              using First row in the UI. Possible values are: AVG,
                              COUNT, COUNT_DISTINCT, MAX, MEDIAN, MIN, STDDEV, SUM'
                            type: string
                          display:
                            description: (string, optional)
                            type: string
                          name:
                            description: (string, required)
                            type: string
                        type: object
                      threshold:
                        description: Threshold to user for alert evaluation, can be
                          a column or a value
                        properties:
                          column:
                            description: (AlertV2OperandColumn, optional)
                            properties:
                              aggregation:
                                description: 'If not set, the behavior is equivalent
                                  to using First row in the UI. Possible values are:
                                  AVG, COUNT, COUNT_DISTINCT, MAX, MEDIAN, MIN, STDDEV,
                                  SUM'
                                type: string
                              display:
                                description: (string, optional)
                                type: string
                              name:
                                description: (string, required)
                                type: string
                            type: object
                          value:
                            description: (AlertV2OperandValue, optional)
                            properties:
                              boolValue:
                                description: (boolean, optional)
                                type: boolean
                              doubleValue:
                                description: (number, optional)
                                type: number
                              stringValue:
                                description: (string, optional)
                                type: string
                            type: object
                        type: object
                    type: object
                  parentPath:
                    description: The workspace path of the folder containing the alert.
                      Can only be set on create, and cannot be updated
                    type: string
                  providerConfig:
                    description: Configure the provider for management through account
                      provider.
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                  purgeOnDelete:
                    description: Whether to permanently delete the alert. If not set,
                      the alert will only be soft deleted
                    type: boolean
                  queryText:
                    description: Text of the query to be run
                    type: string
                  runAs:
                    description: |-
                      Specifies the identity that will be used to run the alert.
                      This field allows you to configure alerts to run as a specific user or service principal.
                    properties:
                      servicePrincipalName:
                        description: |-
                          to the application ID. Requires the servicePrincipal/user role.
                          If not specified, the alert will run as the request user
                        type: string
                      userName:
                        description: to the email of an active workspace user. Users
                          can only set this to their own email.
                        type: string
                    type: object
                  runAsUserName:
                    description: |-
                      The run as username or application ID of service principal.
                      On Create and Update, this field can be set to application ID of an active service principal. Setting this field requires the servicePrincipal/user role.
                      Deprecated: Use run_as field instead. This field will be removed in a future release
                    type: string
                  schedule:
                    description: (CronSchedule, required)
                    properties:
                      pauseStatus:
                        description: 'Indicate whether this schedule is paused or
                          not. Possible values are: PAUSED, UNPAUSED'
                        type: string
                      quartzCronSchedule:
                        description: |-
                          A cron expression using quartz syntax that specifies the schedule for this pipeline.
                          Should use the quartz format described here: http://www.quartz-scheduler.org/documentation/quartz-2.1.7/tutorials/tutorial-lesson-06.html
                        type: string
                      timezoneId:
                        description: |-
                          A Java timezone id. The schedule will be resolved using this timezone.
                          This will be combined with the quartz_cron_schedule to determine the schedule.
                          See https://docs.databricks.com/sql/language-manual/sql-ref-syntax-aux-conf-mgmt-set-timezone.html for details
                        type: string
                    type: object
                  warehouseId:
                    description: ID of the SQL warehouse attached to the alert
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.displayName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.displayName)
                || (has(self.initProvider) && has(self.initProvider.displayName))'
            - message: spec.forProvider.evaluation is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.evaluation)
                || (has(self.initProvider) && has(self.initProvider.evaluation))'
            - message: spec.forProvider.queryText is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.queryText)
                || (has(self.initProvider) && has(self.initProvider.queryText))'
            - message: spec.forProvider.schedule is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.schedule)
                || (has(self.initProvider) && has(self.initProvider.schedule))'
            - message: spec.forProvider.warehouseId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.warehouseId)
                || (has(self.initProvider) && has(self.initProvider.warehouseId))'
          status:
            description: AlertV2Status defines the observed state of AlertV2.
            properties:
              atProvider:
                properties:
                  createTime:
                    description: The timestamp indicating when the alert was created
                    type: string
                  customDescription:
                    description: Custom description for the alert. support mustache
                      template
                    type: string
                  customSummary:
                    description: Custom summary for the alert. support mustache template
                    type: string
                  displayName:
                    description: The display name of the alert
                    type: string
                  effectiveRunAs:
                    description: |-
                      The actual identity that will be used to execute the alert.
                      This is an output-only field that shows the resolved run-as identity after applying
                      permissions and defaults
                    properties:
                      servicePrincipalName:
                        description: |-
                          to the application ID. Requires the servicePrincipal/user role.
                          If not specified, the alert will run as the request user
                        type: string
                      userName:
                        description: to the email of an active workspace user. Users
                          can only set this to their own email.
                        type: string
                    type: object
                  evaluation:
                    description: (AlertV2Evaluation, required)
                    properties:
                      comparisonOperator:
                        description: 'Operator used for comparison in alert evaluation.
                          Possible values are: EQUAL, GREATER_THAN, GREATER_THAN_OR_EQUAL,
                          IS_NOT_NULL, IS_NULL, LESS_THAN, LESS_THAN_OR_EQUAL, NOT_EQUAL'
                        type: string
                      emptyResultState:
                        description: 'Alert state if result is empty. Please avoid
                          setting this field to be UNKNOWN because UNKNOWN state is
                          planned to be deprecated. Possible values are: ERROR, OK,
                          TRIGGERED, UNKNOWN'
                        type: string
                      lastEvaluatedAt:
                        description: Timestamp of the last evaluation
                        type: string
                      notification:
                        description: User or Notification Destination to notify when
                          alert is triggered
                        properties:
                          effectiveNotifyOnOk:
                            description: Whether to notify alert subscribers when
                              alert returns back to normal
                            type: boolean
                          effectiveRetriggerSeconds:
                            description: |-
                              Number of seconds an alert waits after being triggered before it is allowed to send another notification.
                              If set to 0 or omitted, the alert will not send any further notifications after the first trigger
                              Setting this value to 1 allows the alert to send a notification on every evaluation where the condition is met, effectively making it always retrigger for notification purposes
                            type: number
                          notifyOnOk:
                            description: Whether to notify alert subscribers when
                              alert returns back to normal
                            type: boolean
                          retriggerSeconds:
                            description: |-
                              Number of seconds an alert waits after being triggered before it is allowed to send another notification.
                              If set to 0 or omitted, the alert will not send any further notifications after the first trigger
                              Setting this value to 1 allows the alert to send a notification on every evaluation where the condition is met, effectively making it always retrigger for notification purposes
                            type: number
                          subscriptions:
                            description: (list of AlertV2Subscription, optional)
                            items:
                              properties:
                                destinationId:
                                  description: (string, optional)
                                  type: string
                                userEmail:
                                  description: (string, optional)
                                  type: string
                              type: object
                            type: array
                        type: object
                      source:
                        description: Source column from result to use to evaluate
                          alert
                        properties:
                          aggregation:
                            description: 'If not set, the behavior is equivalent to
                              using First row in the UI. Possible values are: AVG,
                              COUNT, COUNT_DISTINCT, MAX, MEDIAN, MIN, STDDEV, SUM'
                            type: string
                          display:
                            description: (string, optional)
                            type: string
                          name:
                            description: (string, required)
                            type: string
                        type: object
                      state:
                        description: 'Latest state of alert evaluation. Possible values
                          are: ERROR, OK, TRIGGERED, UNKNOWN'
                        type: string
                      threshold:
                        description: Threshold to user for alert evaluation, can be
                          a column or a value
                        properties:
                          column:
                            description: (AlertV2OperandColumn, optional)
                            properties:
                              aggregation:
                                description: 'If not set, the behavior is equivalent
                                  to using First row in the UI. Possible values are:
                                  AVG, COUNT, COUNT_DISTINCT, MAX, MEDIAN, MIN, STDDEV,
                                  SUM'
                                type: string
                              display:
                                description: (string, optional)
                                type: string
                              name:
                                description: (string, required)
                                type: string
                            type: object
                          value:
                            description: (AlertV2OperandValue, optional)
                            properties:
                              boolValue:
                                description: (boolean, optional)
                                type: boolean
                              doubleValue:
                                description: (number, optional)
                                type: number
                              stringValue:
                                description: (string, optional)
                                type: string
                            type: object
                        type: object
                    type: object
                  id:
                    description: UUID identifying the alert
                    type: string
                  lifecycleState:
                    description: 'Indicates whether the query is trashed. Possible
                      values are: ACTIVE, DELETED'
                    type: string
                  ownerUserName:
                    description: The owner's username. This field is set to "Unavailable"
                      if the user has been deleted
                    type: string
                  parentPath:
                    description: The workspace path of the folder containing the alert.
                      Can only be set on create, and cannot be updated
                    type: string
                  providerConfig:
                    description: Configure the provider for management through account
                      provider.
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                  purgeOnDelete:
                    description: Whether to permanently delete the alert. If not set,
                      the alert will only be soft deleted
                    type: boolean
                  queryText:
                    description: Text of the query to be run
                    type: string
                  runAs:
                    description: |-
                      Specifies the identity that will be used to run the alert.
                      This field allows you to configure alerts to run as a specific user or service principal.
                    properties:
                      servicePrincipalName:
                        description: |-
                          to the application ID. Requires the servicePrincipal/user role.
                          If not specified, the alert will run as the request user
                        type: string
                      userName:
                        description: to the email of an active workspace user. Users
                          can only set this to their own email.
                        type: string
                    type: object
                  runAsUserName:
                    description: |-
                      The run as username or application ID of service principal.
                      On Create and Update, this field can be set to application ID of an active service principal. Setting this field requires the servicePrincipal/user role.
                      Deprecated: Use run_as field instead. This field will be removed in a future release
                    type: string
                  schedule:
                    description: (CronSchedule, required)
                    properties:
                      pauseStatus:
                        description: 'Indicate whether this schedule is paused or
                          not. Possible values are: PAUSED, UNPAUSED'
                        type: string
                      quartzCronSchedule:
                        description: |-
                          A cron expression using quartz syntax that specifies the schedule for this pipeline.
                          Should use the quartz format described here: http://www.quartz-scheduler.org/documentation/quartz-2.1.7/tutorials/tutorial-lesson-06.html
                        type: string
                      timezoneId:
                        description: |-
                          A Java timezone id. The schedule will be resolved using this timezone.
                          This will be combined with the quartz_cron_schedule to determine the schedule.
                          See https://docs.databricks.com/sql/language-manual/sql-ref-syntax-aux-conf-mgmt-set-timezone.html for details
                        type: string
                    type: object
                  updateTime:
                    description: The timestamp indicating when the alert was updated
                    type: string
                  warehouseId:
                    description: ID of the SQL warehouse attached to the alert
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
