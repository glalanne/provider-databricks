---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: environmentsworkspacebaseenvironments.envs.databricks.m.crossplane.io
spec:
  group: envs.databricks.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - databricks
    kind: EnvironmentsWorkspaceBaseEnvironment
    listKind: EnvironmentsWorkspaceBaseEnvironmentList
    plural: environmentsworkspacebaseenvironments
    singular: environmentsworkspacebaseenvironment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: EnvironmentsWorkspaceBaseEnvironment is the Schema for the EnvironmentsWorkspaceBaseEnvironments
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EnvironmentsWorkspaceBaseEnvironmentSpec defines the desired
              state of EnvironmentsWorkspaceBaseEnvironment
            properties:
              forProvider:
                properties:
                  baseEnvironmentType:
                    description: 'The type of base environment (CPU or GPU). Possible
                      values are: CPU, GPU'
                    type: string
                  displayName:
                    description: Human-readable display name for the workspace base
                      environment
                    type: string
                  filepath:
                    description: The WSFS or UC Volumes path to the environment YAML
                      file
                    type: string
                  providerConfig:
                    description: Configure the provider for management through account
                      provider.
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                  workspaceBaseEnvironmentId:
                    description: |-
                      The ID to use for the workspace base environment, which will become the final component of
                      the resource name.
                      This value should be 4-63 characters, and valid characters are /[a-z][0-9]-/
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  baseEnvironmentType:
                    description: 'The type of base environment (CPU or GPU). Possible
                      values are: CPU, GPU'
                    type: string
                  displayName:
                    description: Human-readable display name for the workspace base
                      environment
                    type: string
                  filepath:
                    description: The WSFS or UC Volumes path to the environment YAML
                      file
                    type: string
                  providerConfig:
                    description: Configure the provider for management through account
                      provider.
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                  workspaceBaseEnvironmentId:
                    description: |-
                      The ID to use for the workspace base environment, which will become the final component of
                      the resource name.
                      This value should be 4-63 characters, and valid characters are /[a-z][0-9]-/
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.displayName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.displayName)
                || (has(self.initProvider) && has(self.initProvider.displayName))'
          status:
            description: EnvironmentsWorkspaceBaseEnvironmentStatus defines the observed
              state of EnvironmentsWorkspaceBaseEnvironment.
            properties:
              atProvider:
                properties:
                  baseEnvironmentType:
                    description: 'The type of base environment (CPU or GPU). Possible
                      values are: CPU, GPU'
                    type: string
                  createTime:
                    description: Timestamp when the environment was created
                    type: string
                  creatorUserId:
                    description: User ID of the creator
                    type: string
                  displayName:
                    description: Human-readable display name for the workspace base
                      environment
                    type: string
                  effectiveBaseEnvironmentType:
                    description: 'The type of base environment (CPU or GPU). Possible
                      values are: CPU, GPU'
                    type: string
                  filepath:
                    description: The WSFS or UC Volumes path to the environment YAML
                      file
                    type: string
                  id:
                    type: string
                  isDefault:
                    description: Whether this is the default environment for the workspace
                    type: boolean
                  lastUpdatedUserId:
                    description: User ID of the last user who updated the environment
                    type: string
                  message:
                    description: Status message providing additional details about
                      the environment status
                    type: string
                  name:
                    description: |-
                      The resource name of the workspace base environment.
                      Format: workspace-base-environments/{workspace-base-environment}
                    type: string
                  providerConfig:
                    description: Configure the provider for management through account
                      provider.
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                  spec:
                    description: The environment specification containing version
                      and dependencies
                    properties:
                      dependencies:
                        description: |-
                          List of pip dependencies, as supported by the version of pip in this environment.
                          Each dependency is a valid pip requirements file line per https://pip.pypa.io/en/stable/reference/requirements-file-format/.
                          Allowed dependencies include a requirement specifier, an archive URL, a local project path (such as WSFS or UC Volumes in Databricks), or a VCS project URL
                        items:
                          type: string
                        type: array
                      environmentVersion:
                        description: |-
                          Environment version used by the environment.
                          Each version comes with a specific Python version and a set of Python packages.
                          The version is a string, consisting of an integer
                        type: string
                    type: object
                  status:
                    description: 'The status of the materialized workspace base environment.
                      Possible values are: CREATED, EXPIRED, FAILED, INVALID, PENDING,
                      REFRESHING'
                    type: string
                  updateTime:
                    description: Timestamp when the environment was last updated
                    type: string
                  workspaceBaseEnvironmentId:
                    description: |-
                      The ID to use for the workspace base environment, which will become the final component of
                      the resource name.
                      This value should be 4-63 characters, and valid characters are /[a-z][0-9]-/
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
