---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: postgresendpoints.postgres.databricks.crossplane.io
spec:
  group: postgres.databricks.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - databricks
    kind: PostgresEndpoint
    listKind: PostgresEndpointList
    plural: postgresendpoints
    singular: postgresendpoint
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: PostgresEndpoint is the Schema for the PostgresEndpoints API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PostgresEndpointSpec defines the desired state of PostgresEndpoint
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  endpointId:
                    description: The part of the name, chosen by the user when the
                      resource was created
                    type: string
                  parent:
                    description: |-
                      The branch containing this endpoint (API resource hierarchy).
                      Format: projects/{project_id}/branches/{branch_id}
                    type: string
                  parentRef:
                    description: Reference to a PostgresBranch in postgres to populate
                      parent.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  parentSelector:
                    description: Selector for a PostgresBranch in postgres to populate
                      parent.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  providerConfig:
                    description: Configure the provider for management through account
                      provider.
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                  replaceExisting:
                    description: If true, update the endpoint if it already exists
                      instead of returning an error
                    type: boolean
                  spec:
                    description: The spec contains the compute endpoint configuration,
                      including autoscaling limits, suspend timeout, and disabled
                      state
                    properties:
                      autoscalingLimitMaxCu:
                        description: |-
                          The maximum number of Compute Units. The maximum value is 64.
                          The difference between the minimum and maximum Compute Units (max - min) must not exceed 16
                        type: number
                      autoscalingLimitMinCu:
                        description: The minimum number of Compute Units. Minimum
                          value is 0.5
                        type: number
                      disabled:
                        description: |-
                          Whether to restrict connections to the compute endpoint.
                          Enabling this option schedules a suspend compute operation.
                          A disabled compute endpoint cannot be enabled by a connection or
                          console action
                        type: boolean
                      endpointType:
                        description: 'The endpoint type. A branch can only have one
                          READ_WRITE endpoint. Possible values are: ENDPOINT_TYPE_READ_ONLY,
                          ENDPOINT_TYPE_READ_WRITE'
                        type: string
                      group:
                        description: |-
                          Settings for optional HA configuration of the endpoint. If unspecified, the endpoint defaults
                          to non HA settings, with a single compute backing the endpoint (and no readable secondaries
                          for Read/Write endpoints)
                        properties:
                          enableReadableSecondaries:
                            description: |-
                              Whether to allow read-only connections to read-write endpoints. Only relevant for read-write endpoints where
                              size.max > 1
                            type: boolean
                          max:
                            description: |-
                              The maximum number of computes in the endpoint group. Currently, this must be equal to min. Set to 1 for single
                              compute endpoints, to disable HA. To manually suspend all computes in an endpoint group, set disabled to
                              true on the EndpointSpec
                            type: number
                          min:
                            description: |-
                              The minimum number of computes in the endpoint group. Currently, this must be equal to max. This must be greater
                              than or equal to 1
                            type: number
                        type: object
                      noSuspension:
                        description: |-
                          When set to true, explicitly disables automatic suspension (never suspend).
                          Should be set to true when provided.
                          Mutually exclusive with suspend_timeout_duration. When updating, use spec.suspension in the update_mask
                        type: boolean
                      settings:
                        description: (EndpointSettings, optional)
                        properties:
                          pgSettings:
                            additionalProperties:
                              type: string
                            description: A raw representation of Postgres settings
                            type: object
                            x-kubernetes-map-type: granular
                        type: object
                      suspendTimeoutDuration:
                        description: |-
                          Duration of inactivity after which the compute endpoint is automatically suspended.
                          If specified should be between 60s and 604800s (1 minute to 1 week).
                          Mutually exclusive with no_suspension. When updating, use spec.suspension in the update_mask
                        type: string
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  endpointId:
                    description: The part of the name, chosen by the user when the
                      resource was created
                    type: string
                  parent:
                    description: |-
                      The branch containing this endpoint (API resource hierarchy).
                      Format: projects/{project_id}/branches/{branch_id}
                    type: string
                  parentRef:
                    description: Reference to a PostgresBranch in postgres to populate
                      parent.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  parentSelector:
                    description: Selector for a PostgresBranch in postgres to populate
                      parent.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  providerConfig:
                    description: Configure the provider for management through account
                      provider.
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                  replaceExisting:
                    description: If true, update the endpoint if it already exists
                      instead of returning an error
                    type: boolean
                  spec:
                    description: The spec contains the compute endpoint configuration,
                      including autoscaling limits, suspend timeout, and disabled
                      state
                    properties:
                      autoscalingLimitMaxCu:
                        description: |-
                          The maximum number of Compute Units. The maximum value is 64.
                          The difference between the minimum and maximum Compute Units (max - min) must not exceed 16
                        type: number
                      autoscalingLimitMinCu:
                        description: The minimum number of Compute Units. Minimum
                          value is 0.5
                        type: number
                      disabled:
                        description: |-
                          Whether to restrict connections to the compute endpoint.
                          Enabling this option schedules a suspend compute operation.
                          A disabled compute endpoint cannot be enabled by a connection or
                          console action
                        type: boolean
                      endpointType:
                        description: 'The endpoint type. A branch can only have one
                          READ_WRITE endpoint. Possible values are: ENDPOINT_TYPE_READ_ONLY,
                          ENDPOINT_TYPE_READ_WRITE'
                        type: string
                      group:
                        description: |-
                          Settings for optional HA configuration of the endpoint. If unspecified, the endpoint defaults
                          to non HA settings, with a single compute backing the endpoint (and no readable secondaries
                          for Read/Write endpoints)
                        properties:
                          enableReadableSecondaries:
                            description: |-
                              Whether to allow read-only connections to read-write endpoints. Only relevant for read-write endpoints where
                              size.max > 1
                            type: boolean
                          max:
                            description: |-
                              The maximum number of computes in the endpoint group. Currently, this must be equal to min. Set to 1 for single
                              compute endpoints, to disable HA. To manually suspend all computes in an endpoint group, set disabled to
                              true on the EndpointSpec
                            type: number
                          min:
                            description: |-
                              The minimum number of computes in the endpoint group. Currently, this must be equal to max. This must be greater
                              than or equal to 1
                            type: number
                        type: object
                      noSuspension:
                        description: |-
                          When set to true, explicitly disables automatic suspension (never suspend).
                          Should be set to true when provided.
                          Mutually exclusive with suspend_timeout_duration. When updating, use spec.suspension in the update_mask
                        type: boolean
                      settings:
                        description: (EndpointSettings, optional)
                        properties:
                          pgSettings:
                            additionalProperties:
                              type: string
                            description: A raw representation of Postgres settings
                            type: object
                            x-kubernetes-map-type: granular
                        type: object
                      suspendTimeoutDuration:
                        description: |-
                          Duration of inactivity after which the compute endpoint is automatically suspended.
                          If specified should be between 60s and 604800s (1 minute to 1 week).
                          Mutually exclusive with no_suspension. When updating, use spec.suspension in the update_mask
                        type: string
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.endpointId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.endpointId)
                || (has(self.initProvider) && has(self.initProvider.endpointId))'
          status:
            description: PostgresEndpointStatus defines the observed state of PostgresEndpoint.
            properties:
              atProvider:
                properties:
                  createTime:
                    description: A timestamp indicating when the compute endpoint
                      was created
                    type: string
                  endpointId:
                    description: The part of the name, chosen by the user when the
                      resource was created
                    type: string
                  id:
                    type: string
                  name:
                    description: |-
                      Output only. The full resource path of the endpoint.
                      Format: projects/{project_id}/branches/{branch_id}/endpoints/{endpoint_id}
                    type: string
                  parent:
                    description: |-
                      The branch containing this endpoint (API resource hierarchy).
                      Format: projects/{project_id}/branches/{branch_id}
                    type: string
                  providerConfig:
                    description: Configure the provider for management through account
                      provider.
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                  replaceExisting:
                    description: If true, update the endpoint if it already exists
                      instead of returning an error
                    type: boolean
                  spec:
                    description: The spec contains the compute endpoint configuration,
                      including autoscaling limits, suspend timeout, and disabled
                      state
                    properties:
                      autoscalingLimitMaxCu:
                        description: |-
                          The maximum number of Compute Units. The maximum value is 64.
                          The difference between the minimum and maximum Compute Units (max - min) must not exceed 16
                        type: number
                      autoscalingLimitMinCu:
                        description: The minimum number of Compute Units. Minimum
                          value is 0.5
                        type: number
                      disabled:
                        description: |-
                          Whether to restrict connections to the compute endpoint.
                          Enabling this option schedules a suspend compute operation.
                          A disabled compute endpoint cannot be enabled by a connection or
                          console action
                        type: boolean
                      endpointType:
                        description: 'The endpoint type. A branch can only have one
                          READ_WRITE endpoint. Possible values are: ENDPOINT_TYPE_READ_ONLY,
                          ENDPOINT_TYPE_READ_WRITE'
                        type: string
                      group:
                        description: |-
                          Settings for optional HA configuration of the endpoint. If unspecified, the endpoint defaults
                          to non HA settings, with a single compute backing the endpoint (and no readable secondaries
                          for Read/Write endpoints)
                        properties:
                          enableReadableSecondaries:
                            description: |-
                              Whether to allow read-only connections to read-write endpoints. Only relevant for read-write endpoints where
                              size.max > 1
                            type: boolean
                          max:
                            description: |-
                              The maximum number of computes in the endpoint group. Currently, this must be equal to min. Set to 1 for single
                              compute endpoints, to disable HA. To manually suspend all computes in an endpoint group, set disabled to
                              true on the EndpointSpec
                            type: number
                          min:
                            description: |-
                              The minimum number of computes in the endpoint group. Currently, this must be equal to max. This must be greater
                              than or equal to 1
                            type: number
                        type: object
                      noSuspension:
                        description: |-
                          When set to true, explicitly disables automatic suspension (never suspend).
                          Should be set to true when provided.
                          Mutually exclusive with suspend_timeout_duration. When updating, use spec.suspension in the update_mask
                        type: boolean
                      settings:
                        description: (EndpointSettings, optional)
                        properties:
                          pgSettings:
                            additionalProperties:
                              type: string
                            description: A raw representation of Postgres settings
                            type: object
                            x-kubernetes-map-type: granular
                        type: object
                      suspendTimeoutDuration:
                        description: |-
                          Duration of inactivity after which the compute endpoint is automatically suspended.
                          If specified should be between 60s and 604800s (1 minute to 1 week).
                          Mutually exclusive with no_suspension. When updating, use spec.suspension in the update_mask
                        type: string
                    type: object
                  status:
                    description: Current operational status of the compute endpoint
                    properties:
                      autoscalingLimitMaxCu:
                        description: |-
                          The maximum number of Compute Units. The maximum value is 64.
                          The difference between the minimum and maximum Compute Units (max - min) must not exceed 16
                        type: number
                      autoscalingLimitMinCu:
                        description: The minimum number of Compute Units. Minimum
                          value is 0.5
                        type: number
                      currentState:
                        description: 'Possible values are: ACTIVE, DEGRADED, IDLE,
                          INIT'
                        type: string
                      disabled:
                        description: |-
                          Whether to restrict connections to the compute endpoint.
                          Enabling this option schedules a suspend compute operation.
                          A disabled compute endpoint cannot be enabled by a connection or
                          console action
                        type: boolean
                      endpointId:
                        description: The part of the name, chosen by the user when
                          the resource was created
                        type: string
                      endpointType:
                        description: 'The endpoint type. A branch can only have one
                          READ_WRITE endpoint. Possible values are: ENDPOINT_TYPE_READ_ONLY,
                          ENDPOINT_TYPE_READ_WRITE'
                        type: string
                      group:
                        description: |-
                          Settings for optional HA configuration of the endpoint. If unspecified, the endpoint defaults
                          to non HA settings, with a single compute backing the endpoint (and no readable secondaries
                          for Read/Write endpoints)
                        properties:
                          enableReadableSecondaries:
                            description: |-
                              Whether to allow read-only connections to read-write endpoints. Only relevant for read-write endpoints where
                              size.max > 1
                            type: boolean
                          max:
                            description: |-
                              The maximum number of computes in the endpoint group. Currently, this must be equal to min. Set to 1 for single
                              compute endpoints, to disable HA. To manually suspend all computes in an endpoint group, set disabled to
                              true on the EndpointSpec
                            type: number
                          min:
                            description: |-
                              The minimum number of computes in the endpoint group. Currently, this must be equal to max. This must be greater
                              than or equal to 1
                            type: number
                        type: object
                      hosts:
                        description: Contains host information for connecting to the
                          endpoint
                        properties:
                          host:
                            description: |-
                              The hostname to connect to this endpoint. For read-write endpoints, this is a read-write hostname which connects
                              to the primary compute. For read-only endpoints, this is a read-only hostname which allows read-only operations
                            type: string
                          readOnlyHost:
                            description: |-
                              An optionally defined read-only host for the endpoint, without pooling. For read-only endpoints,
                              this attribute is always defined and is equivalent to host. For read-write endpoints, this attribute is defined
                              if the enclosing endpoint is a group with greater than 1 computes configured, and has readable secondaries enabled
                            type: string
                          readOnlyPooledHost:
                            description: |-
                              The read-only hostname of the compute endpoint, with pooling. This attribute is always defined for read-only endpoints,
                              and may be defined for read-write endpoints if configured with read replicas and allow read-only connections
                            type: string
                          readWritePooledHost:
                            description: The read-write hostname of the compute endpoint,
                              with pooling. This attribute is only defined for read-write
                              endpoints
                            type: string
                        type: object
                      lastActiveTime:
                        description: A timestamp indicating when the compute endpoint
                          was last active
                        type: string
                      pendingState:
                        description: 'Possible values are: ACTIVE, DEGRADED, IDLE,
                          INIT'
                        type: string
                      settings:
                        description: (EndpointSettings, optional)
                        properties:
                          pgSettings:
                            additionalProperties:
                              type: string
                            description: A raw representation of Postgres settings
                            type: object
                            x-kubernetes-map-type: granular
                        type: object
                      suspendTimeoutDuration:
                        description: |-
                          Duration of inactivity after which the compute endpoint is automatically suspended.
                          If specified should be between 60s and 604800s (1 minute to 1 week).
                          Mutually exclusive with no_suspension. When updating, use spec.suspension in the update_mask
                        type: string
                    type: object
                  uid:
                    description: System-generated unique ID for the endpoint
                    type: string
                  updateTime:
                    description: A timestamp indicating when the compute endpoint
                      was last updated
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
