---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: postgresprojects.postgres.databricks.m.crossplane.io
spec:
  group: postgres.databricks.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - databricks
    kind: PostgresProject
    listKind: PostgresProjectList
    plural: postgresprojects
    singular: postgresproject
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: PostgresProject is the Schema for the PostgresProjects API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PostgresProjectSpec defines the desired state of PostgresProject
            properties:
              forProvider:
                properties:
                  initialBranchSpec:
                    description: |-
                      Configuration for the initial default branch created as part of project creation.
                      Allows overriding branch protection. These settings only apply at creation time
                      and do not affect resources created after project creation
                    properties:
                      isProtected:
                        description: Whether the initial default branch should be
                          protected from deletion
                        type: boolean
                    type: object
                  initialEndpointSpec:
                    description: |-
                      Configuration settings for the initial Read/Write endpoint created inside the initial branch for a newly
                      created project. If omitted, the initial endpoint created will have default settings, without high availability
                      configured. This field does not apply to any endpoints created after project creation. Use
                      spec.default_endpoint_settings to configure default settings for endpoints created after project creation
                    properties:
                      autoscalingLimitMaxCu:
                        description: The maximum number of Compute Units for the initial
                          endpoint
                        type: number
                      autoscalingLimitMinCu:
                        description: The minimum number of Compute Units for the initial
                          endpoint
                        type: number
                      group:
                        description: Settings for HA configuration of the endpoint
                        properties:
                          enableReadableSecondaries:
                            description: |-
                              Whether to allow read-only connections to read-write endpoints. Only relevant for read-write endpoints where
                              size.max > 1
                            type: boolean
                          max:
                            description: |-
                              The maximum number of computes in the endpoint group. Currently, this must be equal to min. Set to 1 for single
                              compute endpoints, to disable HA. To manually suspend all computes in an endpoint group, set disabled to
                              true on the EndpointSpec
                            type: number
                          min:
                            description: |-
                              The minimum number of computes in the endpoint group. Currently, this must be equal to max. This must be greater
                              than or equal to 1
                            type: number
                        type: object
                      noSuspension:
                        description: |-
                          When set to true, explicitly disables automatic suspension (never suspend).
                          Should be set to true when provided.
                          Mutually exclusive with suspend_timeout_duration
                        type: boolean
                      suspendTimeoutDuration:
                        description: |-
                          Duration of inactivity after which the initial endpoint is automatically suspended.
                          If specified, should be between 60s and 604800s (1 minute to 1 week).
                          Mutually exclusive with no_suspension
                        type: string
                    type: object
                  projectId:
                    description: The part of the name, chosen by the user when the
                      resource was created
                    type: string
                  providerConfig:
                    description: Configure the provider for management through account
                      provider.
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                  purgeOnDelete:
                    description: |-
                      If true, permanently deletes the project (hard delete).
                      If false or unset, performs a soft delete
                    type: boolean
                  spec:
                    description: what you want the resource to be.
                    properties:
                      budgetPolicyId:
                        description: |-
                          The desired budget policy to associate with the project.
                          See status.budget_policy_id for the policy that is actually applied to the project
                        type: string
                      customTags:
                        description: |-
                          Custom tags to associate with the project. Forwarded to LBM for billing and cost tracking.
                          To update tags, provide the new tag list and include "spec.custom_tags" in the update_mask.
                          To clear all tags, provide an empty list and include "spec.custom_tags" in the update_mask.
                          To preserve existing tags, omit this field from the update_mask (or use wildcard "*" which auto-excludes empty tags)
                        items:
                          properties:
                            key:
                              description: The key of the custom tag
                              type: string
                            value:
                              description: The value of the custom tag
                              type: string
                          type: object
                        type: array
                      defaultBranch:
                        description: |-
                          The full resource path for the default branch of the project
                          Format: projects/{project_id}/branches/{branch_id}
                        type: string
                      defaultEndpointSettings:
                        description: (ProjectDefaultEndpointSettings, optional)
                        properties:
                          autoscalingLimitMaxCu:
                            description: The maximum number of Compute Units for the
                              initial endpoint
                            type: number
                          autoscalingLimitMinCu:
                            description: The minimum number of Compute Units for the
                              initial endpoint
                            type: number
                          noSuspension:
                            description: |-
                              When set to true, explicitly disables automatic suspension (never suspend).
                              Should be set to true when provided.
                              Mutually exclusive with suspend_timeout_duration
                            type: boolean
                          pgSettings:
                            additionalProperties:
                              type: string
                            description: A raw representation of Postgres settings
                            type: object
                            x-kubernetes-map-type: granular
                          suspendTimeoutDuration:
                            description: |-
                              Duration of inactivity after which the initial endpoint is automatically suspended.
                              If specified, should be between 60s and 604800s (1 minute to 1 week).
                              Mutually exclusive with no_suspension
                            type: string
                        type: object
                      displayName:
                        description: Human-readable project name. Length should be
                          between 1 and 256 characters
                        type: string
                      enablePgNativeLogin:
                        description: Whether to enable PG native password login on
                          all endpoints in this project. Defaults to false
                        type: boolean
                      historyRetentionDuration:
                        description: The number of seconds to retain the shared history
                          for point in time recovery for all branches in this project.
                          Value should be between 172800s (2 days) and 3024000s (35
                          days)
                        type: string
                      pgVersion:
                        description: The major Postgres version number. The set of
                          supported versions may vary; consult the API documentation
                          for currently accepted values
                        type: number
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  initialBranchSpec:
                    description: |-
                      Configuration for the initial default branch created as part of project creation.
                      Allows overriding branch protection. These settings only apply at creation time
                      and do not affect resources created after project creation
                    properties:
                      isProtected:
                        description: Whether the initial default branch should be
                          protected from deletion
                        type: boolean
                    type: object
                  initialEndpointSpec:
                    description: |-
                      Configuration settings for the initial Read/Write endpoint created inside the initial branch for a newly
                      created project. If omitted, the initial endpoint created will have default settings, without high availability
                      configured. This field does not apply to any endpoints created after project creation. Use
                      spec.default_endpoint_settings to configure default settings for endpoints created after project creation
                    properties:
                      autoscalingLimitMaxCu:
                        description: The maximum number of Compute Units for the initial
                          endpoint
                        type: number
                      autoscalingLimitMinCu:
                        description: The minimum number of Compute Units for the initial
                          endpoint
                        type: number
                      group:
                        description: Settings for HA configuration of the endpoint
                        properties:
                          enableReadableSecondaries:
                            description: |-
                              Whether to allow read-only connections to read-write endpoints. Only relevant for read-write endpoints where
                              size.max > 1
                            type: boolean
                          max:
                            description: |-
                              The maximum number of computes in the endpoint group. Currently, this must be equal to min. Set to 1 for single
                              compute endpoints, to disable HA. To manually suspend all computes in an endpoint group, set disabled to
                              true on the EndpointSpec
                            type: number
                          min:
                            description: |-
                              The minimum number of computes in the endpoint group. Currently, this must be equal to max. This must be greater
                              than or equal to 1
                            type: number
                        type: object
                      noSuspension:
                        description: |-
                          When set to true, explicitly disables automatic suspension (never suspend).
                          Should be set to true when provided.
                          Mutually exclusive with suspend_timeout_duration
                        type: boolean
                      suspendTimeoutDuration:
                        description: |-
                          Duration of inactivity after which the initial endpoint is automatically suspended.
                          If specified, should be between 60s and 604800s (1 minute to 1 week).
                          Mutually exclusive with no_suspension
                        type: string
                    type: object
                  projectId:
                    description: The part of the name, chosen by the user when the
                      resource was created
                    type: string
                  providerConfig:
                    description: Configure the provider for management through account
                      provider.
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                  purgeOnDelete:
                    description: |-
                      If true, permanently deletes the project (hard delete).
                      If false or unset, performs a soft delete
                    type: boolean
                  spec:
                    description: what you want the resource to be.
                    properties:
                      budgetPolicyId:
                        description: |-
                          The desired budget policy to associate with the project.
                          See status.budget_policy_id for the policy that is actually applied to the project
                        type: string
                      customTags:
                        description: |-
                          Custom tags to associate with the project. Forwarded to LBM for billing and cost tracking.
                          To update tags, provide the new tag list and include "spec.custom_tags" in the update_mask.
                          To clear all tags, provide an empty list and include "spec.custom_tags" in the update_mask.
                          To preserve existing tags, omit this field from the update_mask (or use wildcard "*" which auto-excludes empty tags)
                        items:
                          properties:
                            key:
                              description: The key of the custom tag
                              type: string
                            value:
                              description: The value of the custom tag
                              type: string
                          type: object
                        type: array
                      defaultBranch:
                        description: |-
                          The full resource path for the default branch of the project
                          Format: projects/{project_id}/branches/{branch_id}
                        type: string
                      defaultEndpointSettings:
                        description: (ProjectDefaultEndpointSettings, optional)
                        properties:
                          autoscalingLimitMaxCu:
                            description: The maximum number of Compute Units for the
                              initial endpoint
                            type: number
                          autoscalingLimitMinCu:
                            description: The minimum number of Compute Units for the
                              initial endpoint
                            type: number
                          noSuspension:
                            description: |-
                              When set to true, explicitly disables automatic suspension (never suspend).
                              Should be set to true when provided.
                              Mutually exclusive with suspend_timeout_duration
                            type: boolean
                          pgSettings:
                            additionalProperties:
                              type: string
                            description: A raw representation of Postgres settings
                            type: object
                            x-kubernetes-map-type: granular
                          suspendTimeoutDuration:
                            description: |-
                              Duration of inactivity after which the initial endpoint is automatically suspended.
                              If specified, should be between 60s and 604800s (1 minute to 1 week).
                              Mutually exclusive with no_suspension
                            type: string
                        type: object
                      displayName:
                        description: Human-readable project name. Length should be
                          between 1 and 256 characters
                        type: string
                      enablePgNativeLogin:
                        description: Whether to enable PG native password login on
                          all endpoints in this project. Defaults to false
                        type: boolean
                      historyRetentionDuration:
                        description: The number of seconds to retain the shared history
                          for point in time recovery for all branches in this project.
                          Value should be between 172800s (2 days) and 3024000s (35
                          days)
                        type: string
                      pgVersion:
                        description: The major Postgres version number. The set of
                          supported versions may vary; consult the API documentation
                          for currently accepted values
                        type: number
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.projectId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.projectId)
                || (has(self.initProvider) && has(self.initProvider.projectId))'
          status:
            description: PostgresProjectStatus defines the observed state of PostgresProject.
            properties:
              atProvider:
                properties:
                  createTime:
                    description: A timestamp indicating when the project was created
                    type: string
                  deleteTime:
                    description: |-
                      A timestamp indicating when the project was soft-deleted.
                      Empty if the project is not deleted, otherwise set to a timestamp in the past
                    type: string
                  id:
                    type: string
                  initialBranchSpec:
                    description: |-
                      Configuration for the initial default branch created as part of project creation.
                      Allows overriding branch protection. These settings only apply at creation time
                      and do not affect resources created after project creation
                    properties:
                      isProtected:
                        description: Whether the initial default branch should be
                          protected from deletion
                        type: boolean
                    type: object
                  initialEndpointSpec:
                    description: |-
                      Configuration settings for the initial Read/Write endpoint created inside the initial branch for a newly
                      created project. If omitted, the initial endpoint created will have default settings, without high availability
                      configured. This field does not apply to any endpoints created after project creation. Use
                      spec.default_endpoint_settings to configure default settings for endpoints created after project creation
                    properties:
                      autoscalingLimitMaxCu:
                        description: The maximum number of Compute Units for the initial
                          endpoint
                        type: number
                      autoscalingLimitMinCu:
                        description: The minimum number of Compute Units for the initial
                          endpoint
                        type: number
                      group:
                        description: Settings for HA configuration of the endpoint
                        properties:
                          enableReadableSecondaries:
                            description: |-
                              Whether to allow read-only connections to read-write endpoints. Only relevant for read-write endpoints where
                              size.max > 1
                            type: boolean
                          max:
                            description: |-
                              The maximum number of computes in the endpoint group. Currently, this must be equal to min. Set to 1 for single
                              compute endpoints, to disable HA. To manually suspend all computes in an endpoint group, set disabled to
                              true on the EndpointSpec
                            type: number
                          min:
                            description: |-
                              The minimum number of computes in the endpoint group. Currently, this must be equal to max. This must be greater
                              than or equal to 1
                            type: number
                        type: object
                      noSuspension:
                        description: |-
                          When set to true, explicitly disables automatic suspension (never suspend).
                          Should be set to true when provided.
                          Mutually exclusive with suspend_timeout_duration
                        type: boolean
                      suspendTimeoutDuration:
                        description: |-
                          Duration of inactivity after which the initial endpoint is automatically suspended.
                          If specified, should be between 60s and 604800s (1 minute to 1 week).
                          Mutually exclusive with no_suspension
                        type: string
                    type: object
                  name:
                    description: |-
                      Output only. The full resource path of the project.
                      Format: projects/{project_id}
                    type: string
                  projectId:
                    description: The part of the name, chosen by the user when the
                      resource was created
                    type: string
                  providerConfig:
                    description: Configure the provider for management through account
                      provider.
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                  purgeOnDelete:
                    description: |-
                      If true, permanently deletes the project (hard delete).
                      If false or unset, performs a soft delete
                    type: boolean
                  purgeTime:
                    description: |-
                      A timestamp indicating when the project is scheduled for permanent deletion.
                      Empty if the project is not deleted, otherwise set to a timestamp in the future
                    type: string
                  spec:
                    description: what you want the resource to be.
                    properties:
                      budgetPolicyId:
                        description: |-
                          The desired budget policy to associate with the project.
                          See status.budget_policy_id for the policy that is actually applied to the project
                        type: string
                      customTags:
                        description: |-
                          Custom tags to associate with the project. Forwarded to LBM for billing and cost tracking.
                          To update tags, provide the new tag list and include "spec.custom_tags" in the update_mask.
                          To clear all tags, provide an empty list and include "spec.custom_tags" in the update_mask.
                          To preserve existing tags, omit this field from the update_mask (or use wildcard "*" which auto-excludes empty tags)
                        items:
                          properties:
                            key:
                              description: The key of the custom tag
                              type: string
                            value:
                              description: The value of the custom tag
                              type: string
                          type: object
                        type: array
                      defaultBranch:
                        description: |-
                          The full resource path for the default branch of the project
                          Format: projects/{project_id}/branches/{branch_id}
                        type: string
                      defaultEndpointSettings:
                        description: (ProjectDefaultEndpointSettings, optional)
                        properties:
                          autoscalingLimitMaxCu:
                            description: The maximum number of Compute Units for the
                              initial endpoint
                            type: number
                          autoscalingLimitMinCu:
                            description: The minimum number of Compute Units for the
                              initial endpoint
                            type: number
                          noSuspension:
                            description: |-
                              When set to true, explicitly disables automatic suspension (never suspend).
                              Should be set to true when provided.
                              Mutually exclusive with suspend_timeout_duration
                            type: boolean
                          pgSettings:
                            additionalProperties:
                              type: string
                            description: A raw representation of Postgres settings
                            type: object
                            x-kubernetes-map-type: granular
                          suspendTimeoutDuration:
                            description: |-
                              Duration of inactivity after which the initial endpoint is automatically suspended.
                              If specified, should be between 60s and 604800s (1 minute to 1 week).
                              Mutually exclusive with no_suspension
                            type: string
                        type: object
                      displayName:
                        description: Human-readable project name. Length should be
                          between 1 and 256 characters
                        type: string
                      enablePgNativeLogin:
                        description: Whether to enable PG native password login on
                          all endpoints in this project. Defaults to false
                        type: boolean
                      historyRetentionDuration:
                        description: The number of seconds to retain the shared history
                          for point in time recovery for all branches in this project.
                          Value should be between 172800s (2 days) and 3024000s (35
                          days)
                        type: string
                      pgVersion:
                        description: The major Postgres version number. The set of
                          supported versions may vary; consult the API documentation
                          for currently accepted values
                        type: number
                    type: object
                  status:
                    description: what the resource actually is.
                    properties:
                      branchLogicalSizeLimitBytes:
                        description: The logical size limit for a branch
                        type: number
                      budgetPolicyId:
                        description: |-
                          The desired budget policy to associate with the project.
                          See status.budget_policy_id for the policy that is actually applied to the project
                        type: string
                      computeLastActiveTime:
                        description: The most recent time when any endpoint of this
                          project was active
                        type: string
                      customTags:
                        description: |-
                          Custom tags to associate with the project. Forwarded to LBM for billing and cost tracking.
                          To update tags, provide the new tag list and include "spec.custom_tags" in the update_mask.
                          To clear all tags, provide an empty list and include "spec.custom_tags" in the update_mask.
                          To preserve existing tags, omit this field from the update_mask (or use wildcard "*" which auto-excludes empty tags)
                        items:
                          properties:
                            key:
                              description: The key of the custom tag
                              type: string
                            value:
                              description: The value of the custom tag
                              type: string
                          type: object
                        type: array
                      defaultBranch:
                        description: |-
                          The full resource path for the default branch of the project
                          Format: projects/{project_id}/branches/{branch_id}
                        type: string
                      defaultEndpointSettings:
                        description: (ProjectDefaultEndpointSettings, optional)
                        properties:
                          autoscalingLimitMaxCu:
                            description: The maximum number of Compute Units for the
                              initial endpoint
                            type: number
                          autoscalingLimitMinCu:
                            description: The minimum number of Compute Units for the
                              initial endpoint
                            type: number
                          noSuspension:
                            description: |-
                              When set to true, explicitly disables automatic suspension (never suspend).
                              Should be set to true when provided.
                              Mutually exclusive with suspend_timeout_duration
                            type: boolean
                          pgSettings:
                            additionalProperties:
                              type: string
                            description: A raw representation of Postgres settings
                            type: object
                            x-kubernetes-map-type: granular
                          suspendTimeoutDuration:
                            description: |-
                              Duration of inactivity after which the initial endpoint is automatically suspended.
                              If specified, should be between 60s and 604800s (1 minute to 1 week).
                              Mutually exclusive with no_suspension
                            type: string
                        type: object
                      displayName:
                        description: Human-readable project name. Length should be
                          between 1 and 256 characters
                        type: string
                      enablePgNativeLogin:
                        description: Whether to enable PG native password login on
                          all endpoints in this project. Defaults to false
                        type: boolean
                      historyRetentionDuration:
                        description: The number of seconds to retain the shared history
                          for point in time recovery for all branches in this project.
                          Value should be between 172800s (2 days) and 3024000s (35
                          days)
                        type: string
                      owner:
                        description: The email of the project owner
                        type: string
                      pgVersion:
                        description: The major Postgres version number. The set of
                          supported versions may vary; consult the API documentation
                          for currently accepted values
                        type: number
                      projectId:
                        description: The part of the name, chosen by the user when
                          the resource was created
                        type: string
                      syntheticStorageSizeBytes:
                        description: The current space occupied by the project in
                          storage
                        type: number
                    type: object
                  uid:
                    description: System-generated unique ID for the project
                    type: string
                  updateTime:
                    description: A timestamp indicating when the project was last
                      updated
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
