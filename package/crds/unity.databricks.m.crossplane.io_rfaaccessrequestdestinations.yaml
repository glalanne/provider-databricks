---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: rfaaccessrequestdestinations.unity.databricks.m.crossplane.io
spec:
  group: unity.databricks.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - databricks
    kind: RfaAccessRequestDestinations
    listKind: RfaAccessRequestDestinationsList
    plural: rfaaccessrequestdestinations
    singular: rfaaccessrequestdestinations
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: RfaAccessRequestDestinations is the Schema for the RfaAccessRequestDestinationss
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RfaAccessRequestDestinationsSpec defines the desired state
              of RfaAccessRequestDestinations
            properties:
              forProvider:
                properties:
                  destinations:
                    description: The access request destinations for the securable
                    items:
                      properties:
                        destinationId:
                          description: |-
                            The identifier for the destination. This is the email address for EMAIL destinations, the URL for URL destinations,
                            or the unique Databricks notification destination ID for all other external destinations
                          type: string
                        destinationType:
                          description: 'The type of the destination. Possible values
                            are: EMAIL, GENERIC_WEBHOOK, MICROSOFT_TEAMS, SLACK, URL'
                          type: string
                        specialDestination:
                          description: |-
                            This field is used to denote whether the destination is the email of the owner of the securable object.
                            The special destination cannot be assigned to a securable and only represents the default destination of the securable.
                            The securable types that support default special destinations are: "catalog", "external_location", "connection", "credential", and "metastore".
                            The destination_type of a special_destination is always EMAIL. Possible values are: SPECIAL_DESTINATION_CATALOG_OWNER, SPECIAL_DESTINATION_CONNECTION_OWNER, SPECIAL_DESTINATION_CREDENTIAL_OWNER, SPECIAL_DESTINATION_EXTERNAL_LOCATION_OWNER, SPECIAL_DESTINATION_METASTORE_OWNER
                          type: string
                      type: object
                    type: array
                  providerConfig:
                    description: Configure the provider for management through account
                      provider.
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                  securable:
                    description: The securable for which the access request destinations
                      are being modified or read
                    properties:
                      fullName:
                        description: |-
                          Required. The full name of the catalog/schema/table.
                          Optional if resource_name is present
                        type: string
                      providerShare:
                        description: |-
                          Optional. The name of the Share object that contains the securable when the securable is
                          getting shared in D2D Delta Sharing
                        type: string
                      type:
                        description: |-
                          Required. The type of securable (catalog/schema/table).
                          Optional if resource_name is present. Possible values are: CATALOG, CLEAN_ROOM, CONNECTION, CREDENTIAL, EXTERNAL_LOCATION, EXTERNAL_METADATA, FUNCTION, METASTORE, PIPELINE, PROVIDER, RECIPIENT, SCHEMA, SHARE, STAGING_TABLE, STORAGE_CREDENTIAL, TABLE, VOLUME
                        type: string
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  destinations:
                    description: The access request destinations for the securable
                    items:
                      properties:
                        destinationId:
                          description: |-
                            The identifier for the destination. This is the email address for EMAIL destinations, the URL for URL destinations,
                            or the unique Databricks notification destination ID for all other external destinations
                          type: string
                        destinationType:
                          description: 'The type of the destination. Possible values
                            are: EMAIL, GENERIC_WEBHOOK, MICROSOFT_TEAMS, SLACK, URL'
                          type: string
                        specialDestination:
                          description: |-
                            This field is used to denote whether the destination is the email of the owner of the securable object.
                            The special destination cannot be assigned to a securable and only represents the default destination of the securable.
                            The securable types that support default special destinations are: "catalog", "external_location", "connection", "credential", and "metastore".
                            The destination_type of a special_destination is always EMAIL. Possible values are: SPECIAL_DESTINATION_CATALOG_OWNER, SPECIAL_DESTINATION_CONNECTION_OWNER, SPECIAL_DESTINATION_CREDENTIAL_OWNER, SPECIAL_DESTINATION_EXTERNAL_LOCATION_OWNER, SPECIAL_DESTINATION_METASTORE_OWNER
                          type: string
                      type: object
                    type: array
                  providerConfig:
                    description: Configure the provider for management through account
                      provider.
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                  securable:
                    description: The securable for which the access request destinations
                      are being modified or read
                    properties:
                      fullName:
                        description: |-
                          Required. The full name of the catalog/schema/table.
                          Optional if resource_name is present
                        type: string
                      providerShare:
                        description: |-
                          Optional. The name of the Share object that contains the securable when the securable is
                          getting shared in D2D Delta Sharing
                        type: string
                      type:
                        description: |-
                          Required. The type of securable (catalog/schema/table).
                          Optional if resource_name is present. Possible values are: CATALOG, CLEAN_ROOM, CONNECTION, CREDENTIAL, EXTERNAL_LOCATION, EXTERNAL_METADATA, FUNCTION, METASTORE, PIPELINE, PROVIDER, RECIPIENT, SCHEMA, SHARE, STAGING_TABLE, STORAGE_CREDENTIAL, TABLE, VOLUME
                        type: string
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.securable is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.securable)
                || (has(self.initProvider) && has(self.initProvider.securable))'
          status:
            description: RfaAccessRequestDestinationsStatus defines the observed state
              of RfaAccessRequestDestinations.
            properties:
              atProvider:
                properties:
                  areAnyDestinationsHidden:
                    description: |-
                      Indicates whether any destinations are hidden from the caller due to a lack of permissions.
                      This value is true if the caller does not have permission to see all destinations
                    type: boolean
                  destinationSourceSecurable:
                    description: |-
                      The source securable from which the destinations are inherited. Either the same value as securable (if destination
                      is set directly on the securable) or the nearest parent securable with destinations set
                    properties:
                      fullName:
                        description: |-
                          Required. The full name of the catalog/schema/table.
                          Optional if resource_name is present
                        type: string
                      providerShare:
                        description: |-
                          Optional. The name of the Share object that contains the securable when the securable is
                          getting shared in D2D Delta Sharing
                        type: string
                      type:
                        description: |-
                          Required. The type of securable (catalog/schema/table).
                          Optional if resource_name is present. Possible values are: CATALOG, CLEAN_ROOM, CONNECTION, CREDENTIAL, EXTERNAL_LOCATION, EXTERNAL_METADATA, FUNCTION, METASTORE, PIPELINE, PROVIDER, RECIPIENT, SCHEMA, SHARE, STAGING_TABLE, STORAGE_CREDENTIAL, TABLE, VOLUME
                        type: string
                    type: object
                  destinations:
                    description: The access request destinations for the securable
                    items:
                      properties:
                        destinationId:
                          description: |-
                            The identifier for the destination. This is the email address for EMAIL destinations, the URL for URL destinations,
                            or the unique Databricks notification destination ID for all other external destinations
                          type: string
                        destinationType:
                          description: 'The type of the destination. Possible values
                            are: EMAIL, GENERIC_WEBHOOK, MICROSOFT_TEAMS, SLACK, URL'
                          type: string
                        specialDestination:
                          description: |-
                            This field is used to denote whether the destination is the email of the owner of the securable object.
                            The special destination cannot be assigned to a securable and only represents the default destination of the securable.
                            The securable types that support default special destinations are: "catalog", "external_location", "connection", "credential", and "metastore".
                            The destination_type of a special_destination is always EMAIL. Possible values are: SPECIAL_DESTINATION_CATALOG_OWNER, SPECIAL_DESTINATION_CONNECTION_OWNER, SPECIAL_DESTINATION_CREDENTIAL_OWNER, SPECIAL_DESTINATION_EXTERNAL_LOCATION_OWNER, SPECIAL_DESTINATION_METASTORE_OWNER
                          type: string
                      type: object
                    type: array
                  fullName:
                    description: |-
                      Required. The full name of the catalog/schema/table.
                      Optional if resource_name is present
                    type: string
                  id:
                    type: string
                  providerConfig:
                    description: Configure the provider for management through account
                      provider.
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                  securable:
                    description: The securable for which the access request destinations
                      are being modified or read
                    properties:
                      fullName:
                        description: |-
                          Required. The full name of the catalog/schema/table.
                          Optional if resource_name is present
                        type: string
                      providerShare:
                        description: |-
                          Optional. The name of the Share object that contains the securable when the securable is
                          getting shared in D2D Delta Sharing
                        type: string
                      type:
                        description: |-
                          Required. The type of securable (catalog/schema/table).
                          Optional if resource_name is present. Possible values are: CATALOG, CLEAN_ROOM, CONNECTION, CREDENTIAL, EXTERNAL_LOCATION, EXTERNAL_METADATA, FUNCTION, METASTORE, PIPELINE, PROVIDER, RECIPIENT, SCHEMA, SHARE, STAGING_TABLE, STORAGE_CREDENTIAL, TABLE, VOLUME
                        type: string
                    type: object
                  securableType:
                    description: The type of the securable
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
