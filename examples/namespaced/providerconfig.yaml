apiVersion: databricks.m.crossplane.io/v1beta1
kind: ProviderConfig
metadata:
  annotations: {}
  name: default
  namespace: upbound-system
spec:
  credentials:
    source: Secret
    secretRef:
      name: provider-secret
      namespace: upbound-system
      key: credentials